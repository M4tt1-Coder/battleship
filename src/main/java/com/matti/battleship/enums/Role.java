package com.matti.battleship.enums;

/** Represents the role of an entity in a networked application. */
public enum Role {
  /** The entity acts as a client. */
  CLIENT,
  /** The entity acts as a server. */
  SERVER
}
